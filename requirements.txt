# sidm-composite-dm-mediator requirements
#
# Generated 2026-08-11 in response to Full Codebase R2 review (T1.4).
# Pinned to exact versions known to work with the wimpy venv that
# produced the D5 results. Re-pin after any major dep upgrade.
#
# Install (on a fresh host):
#   python -m venv wimpy
#   source wimpy/bin/activate          # bash/zsh
#   pip install -r requirements.txt
#
# Optional (for full DSMC + Julia integration):
#   juliaup add 1.11.5                # install Julia 1.11.5
#   juliaup default 1.11.5
#   julia +1.11.5 -e 'using Pkg; Pkg.activate("KiSS-SIDM"); Pkg.instantiate()'

# Core scientific stack (required)
numpy==2.4.6
scipy==1.18.0
matplotlib==3.11.0
pandas==3.0.3

# Bayesian inference (required)
dynesty==3.0.0
emcee==3.1.6

# Testing (required)
pytest==9.1.1

# Documentation & delivery (required for the Telegram PDF pipeline)
fpdf2==2.8.7

# Jupyter (optional, for the notebook-based analysis paths in v0.1-prelim)
# These are present in the wimpy venv but not strictly required for D5+
# Uncomment if you need to run v0.1-prelim notebooks.
# jupyter==1.1.1
# jupyterlab==4.6.0

# Cosmology (optional, only for camb / cobaya / cosmopass paths)
# These were present in the wimpy venv from prior cosmological work.
# Uncomment only if you intend to run the cosmological analyses.
# camb==1.6.6
# cobaya==3.6.2
# cosmopass==1.1
# astropy==8.0.0
# getdist==1.7.7